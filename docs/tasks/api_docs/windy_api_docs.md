# Windy API Documentation

## 📍 Point Forecast API

### 개요
특정 좌표의 기계 판독 가능한 날씨 예보 데이터를 제공하는 API입니다.

### 시작하기
1. [Point Forecast API 키 획득](https://api.windy.com/keys)
2. `https://api.windy.com/api/point-forecast/v2`로 POST 요청 전송

### 요청 (Request)

#### 엔드포인트
```
POST https://api.windy.com/api/point-forecast/v2
```

#### 요청 본문 구조
```json
{
    "lat": 49.809,
    "lon": 16.787,
    "model": "desired_model",
    "parameters": ["desired_parameter_1", "desired_parameter_2", ...],
    "levels": ["optionally_desired_gh_level_1", ...],
    "key": "your_API_key"
}
```

#### 요청 예시
```json
{
    "lat": 49.809,
    "lon": 16.787,
    "model": "gfs",
    "parameters": ["wind", "dewpoint", "rh", "pressure"],
    "levels": ["surface", "800h", "300h"],
    "key": "windyisthebest"
}
```

### 매개변수 상세

#### **lat/lon**
- 좌표의 숫자 값 (예: 49.809 또는 -168)
- 자동으로 소수점 2자리로 반올림 (최대 오차 ~1km 또는 0.7마일)

#### **model** - 예보 모델
| 모델 | 커버리지 |
|------|----------|
| `arome` | 프랑스 및 주변 지역 |
| `iconEU` | 유럽 및 주변 지역 |
| `gfs` | 전 세계 |
| `gfsWave` | 전 세계 (허드슨 베이, 흑해, 카스피해, 북극해 대부분 제외) |
| `namConus` | 미국 및 주변 지역 (캐나다, 멕시코) |
| `namHawaii` | 하와이 |
| `namAlaska` | 알래스카 및 주변 지역 |
| `cams` | 전 세계 |

#### **levels** - 지오포텐셜 레벨
- 기본값: `["surface"]`
- 허용값: `surface`, `1000h`, `950h`, `925h`, `900h`, `850h`, `800h`, `700h`, `600h`, `500h`, `400h`, `300h`, `200h`, `150h`
- 적용 가능 매개변수: `wind`, `dewpoint`, `temp`, `gh`, `rh`

#### **parameters** - 예보 속성

##### 모델별 지원 매개변수
| 매개변수 | arome | iconEu | gfs | gfsWave | namConus | namHawaii | namAlaska | cams |
|---------|-------|--------|-----|---------|----------|-----------|-----------|------|
| `temp` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `dewpoint` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `precip` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `convPrecip` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `snowPrecip` | | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `wind` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `windGust` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `cape` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `ptype` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `lclouds` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `mclouds` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `hclouds` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `rh` | ✓ | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `gh` | | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `pressure` | | ✓ | ✓ | | ✓ | ✓ | ✓ | |
| `waves` | | | | ✓ | | | | |
| `windWaves` | | | | ✓ | | | | |
| `swell1` | | | | ✓ | | | | |
| `swell2` | | | | ✓ | | | | |
| `so2sm` | | | | | | | | ✓ |
| `dustsm` | | | | | | | | ✓ |
| `cosc` | | | | | | | | ✓ |

#### **key**
- 사용자가 생성한 특정 API 키
- 다른 서비스(예: map forecast, webcams)용 키와는 구별됨

### 응답 (Response)

#### HTTP 상태 코드
- `200`: 정상 처리
- `204`: 선택한 모델이 요청한 매개변수를 지원하지 않음
- `400`: 잘못된 요청
- `500`: 예상치 못한 오류

#### 응답 본문 구조
```json
{
    "ts": [int],
    "units": {
        "{parameter-level}": "string",
        "{parameter2-level}": "string"
    },
    "{parameter-level}": [float],
    "{parameter2-level}": [float]
}
```

### 매개변수 상세 설명

| 매개변수 | 응답 키 | 설명 |
|---------|---------|------|
| `temp` | `temp-[level]` | 기온 |
| `dewpoint` | `dewpoint-[level]` | 이슬점 온도 (습도 100%가 되는 온도) |
| `precip` | `past3hprecip-surface` | 지난 3시간 총 강수량 |
| `snowPrecip` | `past3hsnowprecip-surface` | 지난 3시간 총 강설량 |
| `convPrecip` | `past3hconvprecip-surface` | 지난 3시간 대류 강수량 |
| `wind` | `wind_u-[level]`, `wind_v-[level]` | 풍속 및 방향 (2차원 벡터) |
| `windGust` | `gust-surface` | 돌풍 속도 |
| `cape` | `cape-surface` | 대류 가용 잠재 에너지 |
| `ptype` | `ptype-surface` | 강수 유형 (0: 없음, 1: 비, 3: 얼어붙는 비, 5: 눈, 7: 비와 눈 혼합, 8: 얼음 알갱이) |
| `lclouds` | `lclouds-surface` | 하층운 (800hPa 이상) |
| `mclouds` | `mclouds-surface` | 중층운 (450-800hPa) |
| `hclouds` | `hclouds-surface` | 상층운 (450hPa 이하) |
| `rh` | `rh-[level]` | 상대 습도 |
| `gh` | `gh-[level]` | 지오포텐셜 고도 |
| `pressure` | `pressure-surface` | 기압 |
| `waves` | `waves_height-surface`, `waves_period-surface`, `waves_direction-surface` | 파고, 주기, 방향 |
| `windWaves` | `wwaves_height-surface`, `wwaves_period-surface`, `wwaves_direction-surface` | 풍파 높이, 주기, 방향 |
| `swell1` | `swell1_height-surface`, `swell1_period-surface`, `swell1_direction-surface` | 너울 1 |
| `swell2` | `swell2_height-surface`, `swell2_period-surface`, `swell2_direction-surface` | 너울 2 |
| `so2sm` | `chem_so2sm-surface` | 이산화황 |
| `dustsm` | `chem_dustsm-surface` | 먼지 입자 |
| `cosc` | `chem_cosc-surface` | 일산화탄소 농도 |

---

## 🗺️ Map Forecast API

### 개요
Leaflet 1.4.x 기반의 간단한 라이브러리로, Windy.com의 지도 시각화를 사용자 정의할 수 있습니다.

### 시작하기
1. [Windy API 키 획득](https://api.windy.com/keys)
2. Leaflet 1.4.0 라이브러리 추가:
   ```html
   <script src="https://unpkg.com/leaflet@1.4.0/dist/leaflet.js"></script>
   ```
3. Windy API 라이브러리 추가:
   ```html
   <script src="https://api.windy.com/assets/map-forecast/libBoot.js"></script>
   ```
   > ⚠️ Leaflet CSS 파일은 사용하지 마세요. Windy API가 수정된 스타일을 자동으로 다운로드합니다.

### 주요 구성 요소

#### **store** - 상태 관리
| 메서드 | 반환값 | 설명 |
|--------|--------|------|
| `get(key)` | any | 키에 설정된 값 반환 |
| `getAllowed(key)` | any[] \| string | 설정 가능한 모든 값의 배열 반환 |
| `set(key, value, opts)` | - | 키의 값 설정 또는 변경 |
| `on(key, callback)` | - | 값 변경 시마다 콜백 실행 |
| `once(key, callback)` | - | 값 변경 시 한 번만 콜백 실행 |
| `off(key, callback)` | - | 콜백 함수 언바인드 |

#### **map** - Leaflet 지도 인스턴스
Leaflet L.Map 클래스의 인스턴스입니다.

#### **picker** - 날씨 선택기
| 메서드 | 반환값 | 설명 |
|--------|--------|------|
| `open({ lat, lon })` | - | 특정 좌표에 선택기 열기 |
| `close()` | - | 선택기 닫기 |
| `getParams()` | `{ lat, lon, values, overlay }` | 열린 선택기의 값 가져오기 |

##### 이벤트
- `pickerOpened`: 선택기가 열릴 때
- `pickerMoved`: 선택기가 이동할 때
- `pickerClosed`: 선택기가 닫힐 때

#### **utils** - 유틸리티 함수
| 메서드 | 반환값 | 설명 |
|--------|--------|------|
| `deg2rad(deg)` | number | 도를 라디안으로 변환 |
| `isNear({lat,lon}, {lat,lon})` | boolean | 두 좌표가 0.01도 이내인지 확인 |
| `isValidLatLonObj({lat,lon})` | boolean | 객체가 lat, lon 속성을 가지는지 확인 |
| `lonDegToXUnit(deg)` | number | 경도를 메르카토르로 변환 |
| `latDegToYUnit(deg)` | number | 위도를 메르카토르로 변환 |
| `wave2obj(number[])` | `{period, dir, size}` | 파도 벡터를 객체로 변환 |
| `wind2obj(number[])` | `{wind, dir}` | 바람 벡터를 객체로 변환 |

#### **broadcast** - 이벤트 브로드캐스터
| 메서드 | 반환값 | 설명 |
|--------|--------|------|
| `fire(event, ...args)` | - | 이벤트 발생 |
| `on(event, callback)` | - | 이벤트 리스너 등록 |
| `once(event, callback)` | - | 일회성 이벤트 리스너 등록 |
| `off(event, callback)` | - | 이벤트 리스너 제거 |

#### **overlays** - 레이어 관리
각 레이어의 메트릭을 변경하고 관리할 수 있습니다.

| 메서드 | 반환값 | 설명 |
|--------|--------|------|
| `convertNumber(value)` | number | 기본 메트릭 값을 현재 선택된 메트릭으로 변환 |
| `convertValue(value)` | string | 단위 레이블이 포함된 값으로 변환 |
| `cycleMetric()` | - | 다음 사용 가능한 메트릭으로 전환 |
| `listMetrics()` | string[] | 모든 사용 가능한 메트릭 목록 반환 |
| `setMetric(metric)` | - | 특정 메트릭 설정 |

#### **colors** - 색상 정의
각 레이어의 색상 정의를 관리합니다.

| 메서드 | 반환값 | 설명 |
|--------|--------|------|
| `changeColor(colors)` | - | 기본 색상 변경 |
| `checkValidity(colors)` | boolean | 색상 스케일 유효성 검사 |
| `color(value)` | string | 값에 대한 RGB 색상 반환 |
| `RGBA(value)` | number[] | 값에 대한 RGBA 배열 반환 |
| `toDefault()` | - | 기본 색상으로 재설정 |

### 사용 조건

#### 테스트 버전 (무료)
- 제한된 모델, 레이어, 등압선만 제공
- 개발 목적으로만 사용 가능
- 프로덕션 사용 불가

#### 프로페셔널 버전
- 모든 기능 사용 가능
- 상업적 사용 가능

### 주의사항
- Leaflet는 `{lat, lng}` 형식을 사용하지만, Windy는 `{lat, lon}` 형식 사용
- 페이지당 하나의 Windy Map 인스턴스만 가능
- `#windy` 선택자 내에 모든 leaflet.css 클래스가 중첩됨
- 전역 CSS 클래스 및 ID 선택자 충돌 주의 (`#bottom`, `#logo` 등)
- 쿠키를 사용하지 않음
- localStorage를 많이 사용함
- 문서화되지 않은 함수 사용 시 업그레이드 시 문제 발생 가능

---

## 📹 Webcams API

### 개요
전 세계 최대 웹캠 저장소에 접근할 수 있는 API로, 국가, 카테고리, 위치 등의 기준으로 웹캠을 필터링할 수 있습니다.

### 시작하기
1. [Webcams API 키 획득](https://api.windy.com/keys)
   > ⏱️ 새로 생성된 API 키가 전 세계적으로 접근 가능해지기까지 몇 분이 소요될 수 있습니다.

2. API 요청 시 헤더에 포함:
   ```
   "x-windy-api-key": "API_KEY"
   ```

### 중요 사항
> ⚠️ **토큰 만료 정책**
> - 무료 API: 10분 후 만료
> - 프로페셔널 API: 24시간 후 만료
> - 토큰 만료 시 HTTP 401 반환
> - 페이지 로드 시마다 API를 호출하는 것을 권장

### API 엔드포인트

#### 지도 클러스터
```
GET /webcams/api/v3/map/clusters
```
바운딩 박스와 줌 레벨에 맞춰 지도에 표시하기 최적화된 웹캠 목록 반환

#### 개별 웹캠 조회
```
GET /webcams/api/v3/webcams/{webcamId}
```
ID로 단일 웹캠 반환

#### 웹캠 목록
```
GET /webcams/api/v3/webcams
```
적용된 수정자에 따른 웹캠 목록 반환

#### 카테고리 목록
```
GET /webcams/api/v3/categories
```
웹캠 필터링에 사용되는 카테고리 목록 반환

#### 전체 웹캠 내보내기
```
GET /webcams/export/all-webcams.json
```
모든 웹캠의 기본 정보가 포함된 JSON 파일 반환

#### 대륙 목록
```
GET /webcams/api/v3/continents
```
특정 대륙을 나타내는 지오 코드 목록 반환

#### 국가 목록
```
GET /webcams/api/v3/countries
```
특정 국가를 나타내는 지오 코드 목록 반환

#### 지역 목록
```
GET /webcams/api/v3/regions
```
특정 지역을 나타내는 지오 코드 목록 반환

### 버전 마이그레이션
API V2를 사용 중인 경우, V3로 전환하기 위한 가이드가 제공됩니다.

---

## 📚 추가 리소스
- [API 키 관리](https://api.windy.com/keys)
- [Leaflet 문서](https://leafletjs.com/)
- [Windy API 커뮤니티](https://community.windy.com/)
- Swagger 문서 (각 API 페이지 하단에서 확인 가능)

## 💡 도움이 필요하신가요?
각 API 문서 페이지에서 상세한 정보와 예제를 확인할 수 있습니다.